{
  "name": "bot-whatsapp-service-desk",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('public', { recursive: true })\"",
    "dev:netlify": "netlify dev"
  },
  "dependencies": {
    "@netlify/blobs": "^9.1.0",
    "@netlify/functions": "^4.2.5"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
